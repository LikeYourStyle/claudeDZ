# Журнал инструментов

> Только дополняется, старые записи не редактируются. Сюда попадает всё, что вы
> добавили в проект или окружение ради этой задачи: библиотеки, CLI-утилиты,
> линтеры, MCP-серверы, плагины и скиллы агента, хуки, внешние сервисы, контейнеры.
>
> Разовый вызов уже установленного инструмента сюда не пишется — он идёт
> в таблицу инструментов внутри файла сессии.
>
> Тупиковая попытка — тоже запись: поставили, не подошло, сняли. Это как раз интересно.

## Шаблон записи

```markdown
## YYYY-MM-DD · Сессия N · <инструмент> vX.Y

- **Тип:** библиотека / CLI / MCP / плагин / скилл / хук / сервис
- **Установка:** `<точная команда>`
- **Зачем:** какую задачу решает
- **Область:** проект или глобально
- **Проверка:** чем убедились, что работает
```

Удалённый или заменённый инструмент фиксируется так же — с пометкой
**удалён** / **заменён на X** и причиной.

---

## 2026-08-14 · Сессия 1 · итог: не установлено ничего

В проект не добавлено ни одной зависимости и ни одной утилиты. Это не «забыли
записать», а решение: задание запрещает БД и внешние сервисы, а всё нужное
уже есть в Node 22 — HTTP-сервер, тест-раннер `node:test`, `crypto.randomUUID()`.
`package.json` без секции `dependencies`, папки `node_modules` в проекте нет.

Ниже — то, что рассматривали и не взяли. Тупики тоже записываем.

## 2026-08-14 · Сессия 1 · poppler-utils (pdftoppm) — **не установлен**

- **Тип:** CLI
- **Установка:** не выполнялась
- **Зачем:** агент не смог открыть `homework.pdf` — для рендера PDF ему нужен
  `pdftoppm`, которого в системе нет.
- **Почему отказались:** ставить системный пакет ради одного чтения задания —
  лишнее. Вместо этого агент написал одноразовый скрипт на Node: распаковал
  потоки PDF через встроенный `zlib` и вытащил текст из операторов `Tj`/`TJ`.
- **Проверка:** текст задания извлёкся, но с побитой кодировкой (сдвиг на +4 по
  ASCII: «Claude Code» читался как «Gpeyhi Gshi»). Пришлось писать второй скрипт-
  дешифратор. Дата сдачи в расшифровке получилась битой — сверяли с `README.md`.
- **Вывод:** сработало, но заняло больше времени, чем ожидалось. Скрипты
  остались во временной папке сессии и в проект не попали.

## 2026-08-14 · Сессия 1 · Express — **отклонён**

- **Тип:** библиотека
- **Установка:** не выполнялась
- **Зачем предлагался:** привычный роутинг вместо ручного разбора `req.url`.
- **Почему отказались:** ради пяти маршрутов появляется `npm install`
  в инструкции запуска и запись про зависимость. Встроенного `node:http`
  хватает, а «ноль зависимостей» заодно автоматически выполняет требование
  задания про отсутствие внешних сервисов.
- **Проверка:** сервис написан на `node:http`, 34 теста проходят.

## 2026-08-14 · Сессия 1 · тест-раннер: встроенный `node:test`

- **Тип:** встроенный инструмент Node 22.19.0
- **Установка:** не требуется
- **Зачем:** автотесты без внешних зависимостей (альтернативы — Jest, Vitest —
  не рассматривались всерьёз именно из-за зависимостей).
- **Проверка:** `npm test` — 34 теста, все проходят.
- **Грабли:** форма `node --test test/` под Windows на Node 22.19 падает
  с `MODULE_NOT_FOUND` — раннер принимает папку за модуль. Рабочая форма —
  глоб: `node --test "test/*.test.js"`. Зафиксировано в `package.json`
  и в `AGENTS.md`, чтобы не «починили» обратно.
