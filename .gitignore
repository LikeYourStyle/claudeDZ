# Зависимостей в проекте нет, но папка не должна появиться случайно
node_modules/

# Рабочее хранилище создаётся при первом запуске.
# Якорь к корню обязателен: без слэша впереди правило спрятало бы и naive/data,
# а снимок наивного прогона должен уехать в репозиторий целиком.
/data/

# Временные файлы атомарной записи — если процесс упал посреди неё
*.tmp-*
